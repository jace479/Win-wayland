# ADR-0001: Use WSLg for Phase 1 GUI Integration

**Status:** Accepted

WSLg is the supported Windows 11 integration layer for Linux GUI windows and
audio. It preserves the host OS boundary and is easier to disable than a
custom display server or shell replacement.
